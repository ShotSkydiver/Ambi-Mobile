ENVMAN_ENVSTORE_PATH='../.envstore.yml'
IOS_VERSION = "${APP_VERSION}"
ANDROID_VERSION_NAME="${APP_VERSION}"
IOS_VERSION_BUILD_NUMBER="${APP_BUILD_NUMBER}"
ANDROID_VERSION_CODE="${APP_BUILD_NUMBER}"

